<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans" 
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" 
	xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-4.2.xsd">
	
	<bean id="sessionFactory" class="org.springframework.orm.hibernate4.LocalSessionFactoryBean">
		<property name="dataSource" ref="dataSource" />
		<property name="hibernateProperties">
			<props>
				<prop key="hibernate.dialect">${orm.hibernate.dialect}</prop>
				<prop key="hibernate.show_sql">${orm.hibernate.show_sql}</prop>
				<prop key="hibernate.format_sql">${orm.hibernate.format_sql}</prop>
				<prop key="hibernate.hbm2ddl.auto">${orm.hibernate.hbm2ddl.auto}</prop>
			</props>
		</property>
		<property name="packagesToScan">
			<list><value>${orm.hibernate.entity.scan.packages}</value></list>
		</property>
	</bean>	
</beans>
